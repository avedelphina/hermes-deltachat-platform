[pytest]
addopts = -v --tb=short -m "not slow"
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: long-running tests (e.g. WebRTC ICE loopback). Run with: pytest -m slow
